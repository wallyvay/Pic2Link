# MAS 1.0.1 (2) 重新提审准备

## 已重新提审：2026-09-11 15:51（北京时间）

- 使用用户指定的 iCloud Drive 私钥原文件，通过 altool `--p8-file-path` 完成认证，不复制/输出私钥；旧 Downloads 路径阻塞已解除。
- 15:34 Apple 服务端 `VERIFY SUCCEEDED with no errors`；15:38 同一哈希 PKG `UPLOAD SUCCEEDED`，Build/Delivery ID `dd0cc813-b4dc-40b3-a68c-6daf4c5b4927`。
- 15:40 精确构建 2 回读 VALID、usesNonExemptEncryption=false；随后选中构建 2。
- 13 语言 Description 已按 QA 哈希限定上传并回读，其余字段保留。审核 notes 保留 OSS 专用凭据和联系人，更新为构建 2 移除所有临时 Apple Events 例外的说明。密钥未进入源码或日志。
- 提交前核对全部 metadata、39 张已完成截图、美国/中国原价格、175 个可售地区、年龄评级、分类、隐私已发布及公开 URL HTTP 200；未更改价格、截图、地区或本机安装。
- 原拒审核项 `resolved=true` 后，版本和审核项变为 READY_FOR_REVIEW，外层提交仍为 UNRESOLVED_ISSUES。首轮保护检查因此停止；只读确认后兼容此状态，再提交原唯一审核项，没有重复创建提交或取消审核。
- 15:50:35 Apple 接受重新提交；15:51:12 独立回读版本和提交均 WAITING_FOR_REVIEW，选中 1.0.1 (2)、VALID、AFTER_APPROVAL、无分阶段发布、仅 1 个正确版本审核项。沿用 Submission ID `91bfa1ba-ec2e-4072-bab1-767635cdb192`。
- 独立证据：`dist/mas-build2-20260911/submission-verification.json`；同目录包含 metadata/screenshots/commerce/description 验证及 preparation 报告。源码语法与 diff 检查通过，无 commit/push。
- 发布技能约束了阶段分离、精确身份、QA 哈希、敏感日志保护和最终回读。等待 Apple 审核不等于已获批准或已公开上架。真实沙盒 Photos/GIF/上传完整端到端验收的既有局限仍保留，不能将设置 UI 测试视为该项通过。

以下“阻塞与剩余步骤”为提审前历史记录，以上述当前结果为准。

目标：Pic2Link: Image Uploader / 6809069103 / Amanoya.Pic2Link / MAC_OS / W7XZ85M98K。用户明确要求按拒审修复计划提审新构建，默认审核通过自动发布。

## 当前结果

- 2026-09-11 14:39 实时 ASC 回读仅构建 1，VALID，选中版本 1.0.1 为 REJECTED，AFTER_APPROVAL。随后将工程所有 CURRENT_PROJECT_VERSION 从 1 更新为 2；正式版本 1.0.1 不变。
- MAS 保留此前移除 Finder 临时 Apple Events 例外的修复，只允许 Photos scripting target。Finder 直接读取在 APP_STORE 下提前拒绝；用户仍可使用拖放、剪贴板或文件选择。
- 修正 Debug 设置 UI 测试入口：不再依赖零尺寸 EmptyView 的 onAppear，通过 AppDelegate 显式承载真实 SettingsView，使用默认设置和无写入回调。该入口不编入 Release，也不证明正式菜单栏入口的完整端到端行为。
- UI 测试兼容 AppKit 静态文本 AXValue、CheckBox 类型及滚动区域可点击判断。最终英文深浅色 2/2 通过，验证压缩与 GIF 开关、MAS Photos/Finder 替代操作提示。结果：`/private/tmp/codex-apple-tests/Pic2Link/review-fix-20260911/Build2SettingsScrollUITests.xcresult`。
- 输入源每次从 `com.tencent.inputmethod.wetype.pinyin` 切至 `com.apple.keylayout.ABC`，结束后恢复并回读；UI 租约已释放。
- MAS 条件单元测试 73/73 通过：`/private/tmp/codex-apple-tests/Pic2Link/mas-build2-20260911/UnitTests.xcresult`。该单测在 Debug 测试入口最后调整前运行；Release 功能路径不受该调整影响。
- Distribution Archive 与 PKG 导出成功；归档和解包后的 App 均通过 codesign deep strict。版本 1.0.1 (2)，arm64/x86_64，Apple Distribution / W7XZ85M98K，无 get-task-allow，无 temporary exception，App Sandbox 开启。
- 加密审计仍为 Apple URLSession TLS、Keychain、CryptoKit 系统认证哈希/HMAC；未新增加密依赖。归档和解包后的 `ITSAppUsesNonExemptEncryption=false`。
- PKG Installer 签名为 `3rd Party Mac Developer Installer: Li Wei (W7XZ85M98K)`。SHA256：`162900931d0baaf2f0079a1eab825b49f73e9509f9440fbf344a7937bda1408a`。

## 产物及命令

- Archive：`dist/mas-build2-20260911/Pic2Link.xcarchive`
- 安装包：`dist/mas-build2-20260911/export/Pic2Link.pkg`
- 导出选项：`docs/release/ExportOptions.plist`，保持原签名策略，不自动更改构建号。
- 使用 `xcodebuild archive`，指定 StoreBuild.xcconfig、独立 ArchiveDerivedData、Manual 签名及既有 Pic2Link Mac App Store profile；随后 `xcodebuild -exportArchive`。
- `ruby -c fastlane/Fastfile`、导出 plist lint、`git diff --check` 通过。未 commit/push、未替换本机安装或读取用户真实照片。

## 阻塞与剩余步骤

ASC 元数据会话仍可用，但旧上传私钥 `/Users/amano/Downloads/AuthKey_3TN9YWQ9Q8.p8` 不存在，`.appstoreconnect/private_keys` 对应链接失效；已在原路径及用户个人资料范围按文件名检查，未找到现有副本。已向用户请求当前本机私钥路径，不索取密钥正文。

尚未完成 Apple 服务端包验证、上传、构建处理与选择、重新提审。收到有效上传认证后，先复核包哈希和实时 ASC 状态，再继续。旧 fastlane release 链硬编码 build 1 和 mas-20260905，禁止直接重跑；需新增或精确调整 build 2 的上传/选择/提审入口。

此前审核专用 OSS 配置已回读成功，保留不变。13 语言移除 Finder 直接读取承诺的描述仍为本地修订，需上传并回读；提交前同步审核说明为新构建的真实行为。新包真实沙盒 Photos/GIF/上传端到端验收仍未完成，不能以单测、UI 设置测试或 OSS CLI 测试代替。
