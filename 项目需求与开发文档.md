# Pic2Link - 项目需求与开发文档

## 一、项目概述

### 1.1 软件名称
**Pic2Link**

### 1.2 软件用途
将剪贴板图片、本地图片或通用文件上传到用户自己的云端存储，快速获取并复制公开链接。

### 1.3 运行平台
- macOS 14.0 (Sonoma) 及以上
- 当前工程默认构建 Apple Silicon；发布通用版本时需另外验证 Intel 架构

### 1.4 软件形态
纯状态栏菜单应用（Menu Bar App），不在 Dock 显示图标。

---

## 二、功能需求

### 2.1 状态栏菜单入口
- 打开软件后，在 Mac 顶部状态栏显示一个图标（photo.on.rectangle）
- 点击图标后向下展开一个列表窗口
- 这是应用的唯一交互入口

### 2.2 待上传图片区域
- 显示当前系统剪切板中最新的一张图片
- 点击图片后开始上传
- 上传过程中在状态栏图标上显示进度；开启压缩时使用左侧压缩、右侧上传的双进度环，未压缩任务仍使用单段上传进度
- 上传完成时发送系统通知（需请求 macOS 通知授权）
- 上传完成时播放轻量成功音效；前台使用时也展示通知横幅
- 上传完成后自动按“Markdown格式链接”开关，将图片 URL 或 Markdown 图片链接复制到剪切板
- 连续触发的图片和文件上传进入同一个先进先出队列；单项失败后继续执行后续任务
- 上传任务在入队时固定所选图床配置，等待期间切换图床不改变既有任务的目标
- 上传任务在入队时同时固定“压缩后上传”设置，等待期间修改尺寸方式不影响既有任务

### 2.3 已上传图片区域
- 显示已经上传到云端图床的图片记录
- 按新旧排列（最新的在前）
- 点击图片或右键“复制链接”，按当前格式开关复制到剪切板；写入成功后播放与上传完成相同的 Glass 提示音
- 默认只显示最近 10 张图片
- 「更多文件」按钮打开独立的历史窗口，窗口展示全部上传记录，支持点击或右键复制链接、右键删除单条和清空全部
- 支持右键删除单条记录
- 支持清空所有历史记录

### 2.3.1 Markdown格式链接
- 菜单栏下拉菜单提供“Markdown格式链接”勾选项，默认关闭并持久化；旧配置缺失此字段时仍复制普通 URL。
- 开启时，上传成功自动复制和历史记录手动复制共用格式：没有文案为 `![](URL)`，带文案为 `![文案](URL)`；关闭时原样复制 URL。
- 新上传的非空文案随对应历史记录保存，无论上传时格式开关是否开启；以后切换开关复制仍可使用原文案。旧记录未保存文案，方括号保持空白。
- 原文案完整保留。生成 Markdown 时换行转为空格，方括号等语法字符进行转义；链接中的括号转义、空白编码，已有 URL 百分号编码和签名参数不改写。
- 复制格式以复制瞬间的开关为准，保存其他设置不会重置菜单内的选择。点击复制只播放成功音效，上传完成沿用原通知与音效，不重复播放两次。
- 勾选或取消该开关时，会用新格式把最近一次上传的链接重新复制到剪切板，并推送一条系统通知；历史为空时不写入剪切板。

### 2.4 图床配置
- 支持多图床配置，并可在菜单栏快速切换当前图床
- 支持阿里云 OSS、Synology C2、京东云、百度云 BOS、腾讯云 COS、七牛、又拍云、Amazon S3、Google Cloud Storage、Cloudflare R2、Backblaze B2、通用 S3、WebDAV、Flickr 和 Imgur
- 根据平台配置 Bucket / 容器、密钥、Endpoint、Region、公开链接前缀等信息
- 提供验证按钮测试连接是否成功
- 支持路径样式选择：
  - 按日期文件夹（推荐）
  - 平铺模式

### 2.4.1 直接上传选中照片
- 固定全局快捷键 **⌘⇧U**，在 Finder 前台时读取所选图片文件，在“照片”App 前台时读取当前所选照片；单张浏览使用“照片”公开接口报告的当前选择。支持多选，不要求先复制。
- 保留独立的剪贴板上传快捷键（默认 **⌘U**）。设置页说明新快捷键用途，禁止把剪贴板快捷键设为 ⌘⇧U；旧配置若占用该组合，优先注册选中照片快捷键并提示另选剪贴板快捷键。
- 菜单提供“上传选中的照片（⇧⌘U）”入口。触发时固定前台应用及 PID，通过只读 Apple Events 获取 Finder selection/URL 或 Photos selection/id，不模拟复制、不读取剪贴板、不调用辅助功能、不扫描照片图库。
- 首次需要用户允许 Pic2Link 对 Finder 或“照片”的自动化访问；“照片”资源另外通过 PhotoKit 获取图库授权。所有权限说明和错误提示覆盖 13 种语言。
- Finder 保留图片原始数据、名称和类型，并沿用文件稳定性检查和大文件确认；非图片文件跳过。“照片”按选中 ID 读取当前编辑版本，按真实数据类型命名，必要时从 iCloud 下载；图库仅仅读取，必须是 PhotoKit 可访问的系统照片图库资源。视频不作为照片上传，读取失败逐项提示。
- Live Photo 在 GIF 开关开启时沿用现有配对视频转换流程，关闭时读取静态图。所有选中照片继续原有文案确认、留空跳过合成、压缩、上传和 Markdown 链接复制流程；图床、文案和压缩选择在触发时固定。
- 空选择、不支持的前台应用、权限拒绝或资源读取失败时明确提示，绝不回退上传剪贴板。再次按键不会叠加尚未完成的选中列表读取请求；已读取的图片照常进入现有队列。

### 2.5 拖拽上传
- 支持从 Finder 直接将图片或普通文件拖拽到状态栏图标上
- 支持从“照片”App 直接拖拽照片；通过公开的文件承诺接口接收照片导出的临时文件，无需用户先手动导出
- 一次拖入多张图片或多个文件时，按 Finder 提供的顺序全部进入上传队列
- 拖拽或从 Finder 复制的图片文件保留原始字节、显示文件名和 MIME 类型，不将 GIF、WebP、HEIC、SVG 等重编码为 PNG。对象存储与 WebDAV 的远端文件名统一在扩展名前增加每次上传独立生成的完整 UUID，签名、请求与返回链接共用同一对象路径，避免同名文件或无名截图加字后覆盖；不修改本地文件名、历史显示名和旧链接。Imgur/Flickr 继续由服务端分配资源 ID。
- 只有截图等没有原始文件的剪贴板位图内容才转换为 PNG 上传
- 对外部截图工具刚写入的文件，先连续两次确认文件大小和修改时间稳定，再复制进内存并签名上传；最长等待 10 秒，仍在写入则提示稍后重试
- 拖拽到可接收区域时高亮状态栏图标，表示可以上传
- 松手后自动上传并显示进度
- 待上传图片区优先展示当前出队图片，并叠加真实字节进度和剩余等待数量；无图片预览的文件显示文件占位卡片

### 2.6 压缩后上传
- 设置中提供“压缩后上传”开关，默认关闭
- 菜单栏下拉菜单提供同一开关的勾选项，可即时开启或关闭并持久化
- 开启后一次只应用一种尺寸方式：按宽、按高、百分比、自由尺寸或最大尺寸
- 按宽、按高和百分比保持宽高比；自由尺寸严格使用指定宽高；最大尺寸只缩小不放大，并以先达到的宽高限制为准
- 图片在上传队列中先于网络请求执行压缩；压缩进度与上传进度分开显示，菜单栏双环由左侧压缩推进到右侧上传
- 压缩结果仅保存在内存中用于本次上传，不写入磁盘、不保存压缩副本、不修改原图
- 动态 GIF 逐帧处理并保留动画；非图片文件不参与压缩，保持原样上传
- 关闭开关时继续沿用原始字节、原文件名和 MIME 类型上传路径

### 2.6.1 实况照片转 GIF
- 在“压缩后上传”配置下提供“实况照片转 GIF”开关，默认关闭；菜单栏下拉菜单也提供同一开关的勾选项，可独立即时开启或关闭
- 开启后，只有剪贴板同时提供内容标识一致的实况照片静图和配对视频时，才在上传前转换为 GIF；静态图片、普通视频或未确认配对关系的内容继续沿用原有上传流程
- 菜单栏还提供“从‘照片’选择实况照片…”入口：系统选择器只显示实况照片。选中后通过 PhotoKit 读取该资源的配对视频；这是“照片”App 复制通常只提供静态封面的替代路径
- GIF 在内存中生成，保留动态画面、不包含音频；按比例压缩到短边不超过 480 像素（480p），不写入磁盘、不保存转换副本，也不修改任何原始资源
- 配对视频仅临时复制到每次任务独有的系统临时目录，GIF 上传结束（成功或失败）立即删除该目录；不会向照片图库写入任何内容
- 转换占用既有“压缩”阶段：面板单独显示转换进度，菜单栏左侧进度环表示转换，完成后右侧进度环表示上传；上传 MIME 类型固定为 `image/gif`

### 2.6.2 上传前添加文案

- 菜单栏提供“上传前添加文案”勾选项，设置页提供同步开关；默认关闭，跨启动保存，旧配置缺少字段时按关闭读取。
- 开启时，剪贴板、快捷键、手动选择、拖放以及转成 GIF 后的实况照片上传共用文案确认入口。普通非图片文件维持原流程。
- 每张图片准备好后，在菜单栏图标下方展开文案面板，显示图片、文件名和独立的多行输入框；多个面板可同时叠加，最新面板在前并自动激活、聚焦输入框。面板失焦不自动取消；关闭前层后聚焦剩余面板。
- Command-Return 或提交按钮确认当前图片，Return 保留换行语义。非空文案先原样复制到剪贴板，再关闭对应面板，按提交顺序进入串行合成/压缩/上传队列。未提交草稿不占上传队列。上传成功仍沿用自动复制链接行为。
- 空串或仅空白字符同样可提交，按钮显示“直接上传”：保留原图数据、文件名和类型，跳过文字合成，继续原有压缩设置与上传流程。空白提交不清空当前剪贴板。
- 点击取消、Escape 或关闭按钮只取消对应图片，不复制其文案、不影响其他草稿或正在上传的任务。文案面板等待期间不会压缩或上传该图片；Live Photo 的既有 GIF 转换先于面板展示，但没有网络上传。
- 用户提交非空文案后使用直接编入 Pic2Link 的毛标标 `WMMarkCore` / `WMMarkRenderer`：照片分析、推荐 editorial 样式、主体避让与可读性评分、Core Text 排版、最终位图合成。原生 macOS Vision 由 `PhotoCaptionRenderer` 适配，不启动毛标标 App，不调用远端 API，不下载字体。
- 文案按输入原样保留；静态图在内存中输出 PNG，再遵守入队时确定的压缩设置和图床配置。GIF 保留帧数、时长和循环次数，按第一帧选定一个固定布局逐帧合成，不随帧重新排版。
- 文案开关与压缩、图床配置一起在入队时固定；等待期间切换开关不改变已入队任务。上传历史缩略图取自最终处理后的内容。
- 非空文案合成时，不能解码的图片、不支持的动画/多页格式、超出资源限制或无法完整排入的文字会明确报错并终止该项；不会静默上传原图。当前限制为 4,000 个 UTF-16 单元、单边 16,384 像素、单帧 4,000 万像素、最多 600 帧且累计 2 亿像素。
- 中间图不保存到磁盘，不修改原始图片；第一版不提供完整编辑器、多图拼版或自动改写文案。
- 构建依赖同级 `../毛标标` 源码目录；仅链接共享模型和渲染库，不链接 UIKit 编辑器。最终 App 无需依赖源码目录或安装毛标标。

### 2.7 数据持久化
- 使用 UserDefaults 存储非敏感配置和历史记录
- 使用 macOS Keychain 按图床配置 UUID 存储 Access Key、Secret、密码、API Key、Token 等认证字段
- 旧版 UserDefaults 明文凭据在首次读取时自动迁移；只有 Keychain 写入成功后才清除明文
- 图床配置本地保存，下次启动自动加载
- 已上传图片列表本地保存，包括缩略图及新增记录的非空文案

### 2.8 多语言
- 支持英语、简体中文、繁体中文、韩语、日语、俄语、西班牙语、葡萄牙语、泰语、印地语、法语、阿拉伯语和德语
- 用户尚未选择语言时使用系统首选语言；不支持的系统语言回退英语
- 用户在设置中选择后立即生效并持久化，以后优先使用用户选择
- SwiftUI、AppKit 菜单、通知和业务错误使用同一语言来源
- 阿拉伯语使用从右到左布局

---

## 三、当前工程结构

```
Pic2Link/
├── README.md                      # 公开仓库说明和构建指南
├── .gitignore                     # Xcode、凭据与构建产物忽略规则
├── LICENSE                        # 保留所有权的源码可见许可声明
├── 项目需求与开发文档.md          # 本文档
├── Pic2Link.xcodeproj/            # Xcode 项目文件
│
├── Pic2Link/                      # 源代码目录
    ├── Pic2LinkApp.swift          # 应用入口、AppDelegate 与菜单栏视图
    │
    ├── Models/
    │   └── ImageModels.swift      # 图片、文件、图床与快捷键模型
    │
    ├── Services/
    │   ├── OSSService.swift       # 多图床上传服务
    │   ├── KeychainService.swift  # 图床凭据 Keychain 读写
    │   ├── LocalizationManager.swift # 运行时语言管理与本地化查询
    │   ├── PasteboardMonitor.swift # 剪切板监听服务
    │   ├── SerialUploadQueue.swift # 图片与文件共用的 FIFO 上传队列
    │   ├── UploadImageCompressor.swift # 内存尺寸计算、逐帧重采样与格式保留
    │   ├── LivePhotoGIFConverter.swift # 已配对实况照片的剪贴板识别与内存 GIF 转换
    │   ├── LivePhotoLibraryPicker.swift # 系统照片选择器与 PhotoKit 资源标识交接
    │   ├── PhotoLibraryLivePhotoSource.swift # 选中实况照片的配对视频临时读取与清理
    │   ├── SelectedPhotoReader.swift # Finder / Photos 只读 Apple Events 选中内容解析
    │   ├── PhotoLibraryImageSource.swift # PhotoKit 读取选中照片当前图像数据
    │   ├── UploadFileReadinessGate.swift # 外部写入文件的稳定性确认
    │   ├── NotificationManager.swift # 系统通知管理
    │   └── StorageService.swift   # 数据持久化服务
    │
    ├── ViewModels/
    │   └── MainViewModel.swift    # 主视图模型（业务逻辑）
    │
    ├── Views/
        ├── ClipboardImageSection.swift     # 待上传图片区域
        ├── UploadedImageSection.swift      # 已上传图片区域
        ├── SettingsButtonSection.swift     # 设置按钮区域
        └── SettingsView.swift              # 图床、快捷键、语言与行为设置
    └── {language}.lproj/
        ├── Localizable.strings    # 13 种语言资源
        └── InfoPlist.strings      # 照片读取与自动化权限说明的 13 种语言资源
│
├── Pic2LinkTests/                 # 语言、凭据迁移、尺寸压缩、进度与错误映射单元测试
└── Pic2LinkUITests/               # 深浅色、压缩设置、RTL 与长文案界面回归测试
```

---

## 四、技术栈

### 4.1 开发语言
- Swift 5.5+

### 4.2 UI 框架
- SwiftUI（主视图）
- AppKit（状态栏管理、拖拽处理、剪切板操作）

### 4.3 核心功能
| 功能 | 实现方式 |
|------|---------|
| 状态栏菜单 | `NSStatusItem` + `NSMenu` + `NSHostingView` |
| 剪切板监听 | `NSPasteboard` + `Timer` |
| 选中照片上传 | Carbon 全局 ⌘⇧U + 只读 `NSAppleEventDescriptor` + PhotoKit 当前图像数据 |
| 文件上传 | `URLSession` + 各图床 REST API |
| 连续上传 | 图片与文件共用串行 FIFO 队列，显示等待数量 |
| 上传前压缩 / 实况 GIF | `ImageIO` + `CoreGraphics` + `AVFoundation`，仅在内存中重采样图片或将已配对实况视频编码为 480p GIF |
| 照片库实况选择 | `PhotosUI` 系统选择器 + `PhotoKit`，仅读取用户选择资源的配对视频到任务临时目录 |
| 新建截图稳定性 | `URLResourceValues` 连续观察文件大小和修改时间，稳定后再建立内存上传载荷 |
| 签名验证 | `CryptoKit`（HMAC-SHA1 / SHA256 / MD5，按平台要求） |
| 数据存储 | `UserDefaults`（非敏感数据）+ macOS Keychain（凭据） |
| 系统通知 | `UserNotifications` |
| 拖拽上传 | `NSDraggingDestination` 协议 |

---

## 五、数据模型

### 5.1 UploadedImage（已上传图片）
```swift
struct UploadedImage: Identifiable, Codable {
    let id: UUID
    let fileName: String        // 文件名
    let url: String             // 完整 URL
    let uploadDate: Date        // 上传时间
    let thumbnailData: Data?    // 缩略图数据
}
```

### 5.2 OSSConfig（OSS 配置）
```swift
struct OSSConfig: Codable {
    var bucketName: String      // 存储桶名称
    var accessKey: String       // Access Key
    var secretKey: String       // Secret Key
    var urlPrefix: String       // 网址前缀
    var endpoint: String        // OSS 节点
    var pathStyle: PathStyle    // 路径样式
}
```

---

## 六、业务流程

### 6.1 剪切板图片上传流程
```
用户复制图片 → PasteboardMonitor 检测到变化 → 发送通知
→ ContentView 更新显示剪切板图片预览 → 用户点击图片
→ MainViewModel.uploadImage() 入队 → 若已开启实况照片转 GIF 且剪贴板资源确认配对，先在内存中转为 480p GIF；否则若压缩开关已开启，按所选尺寸在内存中压缩
→ 压缩完成后调用 OSSService.uploadImage() 并显示网络上传进度
→ 生成签名 → PUT 请求上传 → 返回 URL → 复制到剪切板 → 发送通知
```

### 6.1.1 从照片库选择实况照片上传流程
```
用户点菜单栏「从“照片”选择实况照片…」→ PhotosUI 仅展示实况照片
→ 选择结果提供 PhotoKit assetIdentifier → PhotoKit 读取配对 MOV 到唯一临时目录（必要时从 iCloud 下载）
→ AVFoundation / ImageIO 在内存生成短边不超过 480px 的 GIF → 进入既有网络上传阶段
→ 无论成功或失败，立即删除临时目录；不修改照片图库或原始资源
```

### 6.2 拖拽上传流程
```
用户拖拽图片到状态栏图标 → StatusItemDropView 接收拖拽
→ Finder 文件 URL 直接进入 MainViewModel.uploadFiles()；“照片”App 的文件承诺完成后进入 MainViewModel.uploadFile()
→ （后续流程同上）
```

### 6.3 OSS 签名流程
```
生成 StringToSign → HMAC-SHA1 签名 (CryptoKit) → Base64 编码
→ 构建 Authorization 头 → 发送 PUT 请求
```

---

## 七、当前已知问题与待完善功能

### 7.1 高优先级
1. **剪切板监听失效** - 已修复 Timer RunLoop 问题，待验证
2. **拖拽上传反馈** - 已添加 ➕ 号和进度显示，待验证
3. **上传进度反馈** - 已关联 URLSession 实际发送字节，状态栏显示真实百分比，面板同时显示已发送量 / 总大小

### 7.2 中优先级
4. **更多图片视图** - ✅ 已于 2026-09-12 实现：菜单「更多文件」打开独立窗口展示全部历史记录，单元格固定 104 pt、列数按窗口宽度计算，见下方 2026-09-12 变更记录

### 7.3 低优先级
5. **菜单栏自定义** - 允许用户更换状态栏图标

### 7.4 macOS 26.5.2 菜单栏归属记录异常（已确认，暂不处理）

#### 现象与触发背景
- 确认环境：macOS 26.5.2 (25F84)。
- 问题出现前曾拔下 Studio Display，然后重新接回外接大屏；这与 macOS 外接显示器拓扑变化后菜单栏布局未正确恢复的系统问题一致。
- 系统设置中 Pic2Link 的“允许在菜单栏显示”已打开，但正式 Bundle ID `Amanoya.Pic2Link` 的图标仍可能肉眼不可见。
- 打开 ChatGPT 的菜单栏开关后，ChatGPT 与 Pic2Link 图标会一起出现；关闭 ChatGPT 的开关后，两者会一起消失。

#### 实测证据
- ChatGPT 菜单栏开关打开时，ChatGPT 与 Pic2Link 的 24×24 状态项坐标均为菜单栏内的 `y=3`，图标肉眼可见。
- 关闭 ChatGPT 菜单栏开关后，macOS 同时将 ChatGPT 与正式 Pic2Link 状态项移到 `y=-1` 的隐藏状态；Pic2Link 进程与 `NSStatusItem` 仍存在，不是 App 退出或入口创建失败。
- 反向关闭 Pic2Link 菜单栏开关时，只有 Pic2Link 消失，ChatGPT 仍然显示，说明异常关联是单向的。
- 使用相同 AppKit 菜单栏入口代码、但更换临时 Bundle ID 的探针 App 进行对照：关闭 ChatGPT 后探针仍保持在 `y=3` 并肉眼可见。

#### 结论与已排除项
- 高度确认为 macOS 控制中心 / 菜单栏持久化数据对正式 `Amanoya.Pic2Link` 的归属记录发生了串联，Pic2Link 被错误挂到 ChatGPT 的菜单栏可见性开关下。
- 已排除 Pic2Link 状态项未创建、图标资源、下拉菜单、安装残留、LaunchServices 多副本，以及 Bartender、Ice、Hidden Bar、Vanilla、Dozer 等第三方菜单栏管理器。
- 完整卸载重装、重启 macOS、更换 `autosaveName`、写入 `NSStatusItem Preferred Position` 都未清除正式 Bundle ID 的系统坏状态；后续不应重复这些已失败的尝试，也不应继续重构 Pic2Link 下拉菜单。

#### 当前决策与后续入口
- 2026-08-25 决定暂不处理；当前可保持 ChatGPT 菜单栏开关打开，作为 Pic2Link 入口的临时规避方式。
- 若后续重新处理，优先评估系统设置中的“还原控制中心…”，并先向用户说明它会重置其他菜单栏项目的显示选择与顺序。
- 不得为规避该系统记录而轻率更换正式 Bundle ID；这会影响 Keychain、登录项、系统权限与后续发布身份。

---

## 八、构建与运行

### 8.1 环境要求
- Xcode 26.4+
- macOS 14.0+ SDK

### 8.2 构建步骤
1. 在 Xcode 中打开 `Pic2Link.xcodeproj`
2. 配置签名（Apple ID）
3. 保持 Deployment Target 为 macOS 14.0 或更高
4. 按 ⌘R 运行

### 8.3 打包发布
1. 切换为 Release 配置
2. Product → Archive
3. 导出应用并签名（如需要分发）

---

## 九、注意事项

### 9.1 安全
- Access Key、Secret、密码、API Key、Token 和 Client ID 存储在 macOS Keychain 中
- UserDefaults 只保存非敏感配置；旧版明文凭据会安全迁移并在成功后清除
- 建议使用 RAM 子账号的最小权限 Access Key
- 不要在代码或 Git 中硬编码密钥

### 9.2 权限
- 应用需要「通知」权限才能发送上传完成提醒
- 首次运行时会自动请求通知授权

### 9.3 限制
- 菜单栏应用无法直接访问剪切板内容（需要手动点击图标后显示）
- 沙盒环境下某些系统 API 可能受限

---

## 十、更新日志

### v1.0.1 (开发中)
- ✅ 增加“压缩后上传”开关，支持按宽、按高、百分比、自由和最大尺寸五种互斥方式
- ✅ 图片压缩结果只在内存中参与当前上传，不保存副本、不修改原图；非图片文件保持原样
- ✅ 动态 GIF 逐帧缩放并保留动画，关闭开关时继续保留原始格式与字节
- ✅ 压缩进度和真实网络上传进度分开显示，菜单栏以左侧压缩、右侧上传的双进度环连续呈现两个阶段
- ✅ 压缩设置跟随上传任务入队快照持久化，旧设置自动迁移为默认关闭
- ✅ 13 种语言同步新增压缩设置、状态和错误文案
- ✅ 31 个 XCTest 覆盖五种尺寸算法、内存处理、GIF 帧保留、原图不变、设置迁移、菜单栏两段状态及原有上传回归
- ✅ 修复菜单栏拖放入口：兼容 Finder 文件 URL 与“照片”App 的文件承诺，并保留原有 FIFO 上传队列
- ✅ 菜单栏下拉菜单新增“压缩后上传”和“实况照片转 GIF”勾选项，与设置页使用同一份持久化配置

### v1.0.0 (开发中)
- ✅ 状态栏菜单应用
- ✅ 阿里云 OSS 上传
- ✅ 剪切板图片监听
- ✅ 拖拽上传到状态栏图标
- ✅ 前台 / 后台上传成功通知与成功音效
- ✅ 基于真实发送字节的上传进度
- ✅ 拖拽与剪贴板图片文件保留 GIF 等原始格式、文件名和 MIME 类型
- ✅ 图片与文件共用串行 FIFO 上传队列，失败隔离并显示等待数量
- ✅ OSS 配置界面
- ✅ 数据持久化
- ✅ Pic2Link「图片框延伸为链环」新图标
- ✅ 13 种语言、系统语言默认、设置内即时切换与阿拉伯语 RTL
- ✅ 图床凭据迁移至 macOS Keychain，并提供失败时不丢失旧凭据的保护
- ✅ 22 个 XCTest 覆盖语言解析、语言持久化、凭据迁移、上传进度、格式保真、多文件拖拽解析、错误映射和队列顺序
- ✅ 4 个 UI XCTest 覆盖英语深浅色、阿拉伯语 RTL，以及德语、法语、俄语长文案
- ✅ 公开仓库 README、忽略规则与敏感信息检查
- ✅ 公开可见、保留所有权的许可证声明

---

**最后更新：** 2026年8月28日
**开发者：** Pic2Link Team

### 2026-09-05 上传前添加文案

新增持久化菜单/设置开关、菜单栏图标下方可叠加的独立文案面板，以及毛标标共享排版渲染接入。面板自动聚焦，⌘Return 先复制非空文案并关闭面板，再按提交顺序上传；留空跳过合成，保留压缩。新增 13 语言文案及草稿隔离、确认/取消、空白直传、输出格式、方向和动画回归测试。验证记录见 `docs/caption-validation.md`；不修改版本号、签名身份或商店配置。

### 2026-09-05 Markdown 链接与复制提示音

新增菜单“Markdown格式链接”持久化勾选项，统一上传后及历史复制格式，并保存新上传文案作为图片替代文本；历史复制成功后播放 Glass 音效。兼容旧配置与旧历史。验证见 `docs/markdown-links-validation.md`。

### 2026-09-06 Mac App Store 发布准备

新增独立商店构建配置、App Sandbox 权限、Finder 只读文件夹授权与书签，以及商店版 SMAppService 登录项路径；普通本地分发保持原配置。补充隐私清单、公开隐私政策、13 语言商店资料和真实视图截图，新增项目内 fastlane 发布入口。普通/APP_STORE 单测分别通过 71 项，Distribution Archive 双架构、签名、沙盒与加密声明已验证。PKG 导出等待系统钥匙串授权，ASC 创建等待用户决定未被占用的商店名；尚未上传构建、定价或提审。价格目标为美国 USD 1.99、中国大陆 CNY 15，默认审核后自动发布。完整证据、涉及文件、交互验收限制与后续步骤见 `docs/release/app-store-connect.md`；本次保留已有未提交改动，不替换本机安装。

### 2026-09-06 08:30 商店记录与构建上传

用户确认商店名 Pic2Link: Image Uploader，已创建 Apple ID 6809069103。13 语言资料、39 张截图、隐私声明、年龄分级、美国 USD 1.99/中国大陆 CNY 15 和 175 地区可售设置均已上传并回读。最终 PKG 签名、双架构、沙盒和加密声明检查通过；Apple 服务端验证及上传成功，1.0.1 (1) 已处理为 VALID 并绑定到待审版本，自动发布方式为 AFTER_APPROVAL，无分阶段发布。尚未提交审核，等待真实审核联系人姓名和含国家区号的电话；实际沙盒交互验收限制仍见发布文档。

### 2026-09-06 09:53 提交 Mac App Store 审核

用户补充真实审核联系资料并明确要求提审；已填写并回读后提交 1.0.1 (1)。版本及唯一审核提交均回读为 WAITING_FOR_REVIEW，构建 VALID，审核通过后自动发布且无分阶段发布。最终远端核验及既有交互验收限制见 `docs/release/app-store-connect.md`。此次未修改 App 源码或构建，仅补齐审核资料、只读隐私核验入口和发布记录。

### 2026-09-07 修复同名上传覆盖

修复剪贴板截图加字后共用 `image-caption.png` 导致后续上传覆盖前图的问题。统一为对象存储及 WebDAV 每次上传生成含完整 UUID 的远端文件名，保留文件字节、类型、显示名和目录设置。74/74 单元测试通过，包含三张加字截图、同名/同内容重复上传、六类后端与两种路径模式的请求级回归；签名双架构 Release 已备份安装并重启本机 App，不改版本号或商店构建。已覆盖旧对象不能自动恢复。文件、证据及验收边界见 `docs/upload-uniqueness-validation.md`。

### 2026-09-11 拒审修复与审核资源准备

商店版删除 Finder 临时 Apple Events 例外，并在发送事件前禁止 Finder 选中项读取；⌘⇧U 仅支持 Photos，Finder 文件改用既有拖放、剪贴板或文件选择器。非商店版保留原功能。13 语言提示、权限用途和商店 Description 已同步修正。商店条件单测 73/73、非商店单测 74/74 通过；签名双架构沙盒验证包不含临时例外。深浅色 UI Tests 均未找到初始设置窗口，不能算界面验收完成。

已创建独立 OSS 审核 Bucket，验证真实样本上传/公开读取和禁止匿名列举，30 天自动清理测试文件；生产图床未改。现有 codex RAM 身份被拒绝 CreateUser，尚不能生成审核专用凭据。ASC 实时状态为 REJECTED，未上传新构建或审核资料，未提审。完整文件、权限范围、费用/数据边界和所需管理员操作见 `docs/release/review-fix-20260911.md`。

### 2026-09-12 更多文件窗口与格式切换重新复制

「更多文件」按钮此前没有接线，现在打开独立历史窗口，展示全部上传记录（不再限于最近 10 张），支持点击/右键复制链接、右键删除单条与清空全部，并随上传、删除、清空和语言切换即时更新；窗口沿用弹出面板的上传单元格，单元格宽度固定为 104 pt，列数按窗口宽度计算，缩略图始终不会超出列宽或互相重叠。

勾选或取消「Markdown格式链接」时，除保存开关外，还会用新格式把最近一次上传的链接重新写入剪切板并推送系统通知；历史为空时不改动剪切板内容。新增本地化键 `notification.linkCopied`（13 语言）。

非商店条件单测 78/78 通过，包含新增的格式切换重新复制、空历史不写入两项，以及 2 项网格列宽不变式测试；未修改版本号、签名配置、entitlements 与商店资料，未提交审核。弹出面板与历史窗口的显示已由用户回图确认恢复正常，详见 `docs/history-window-and-link-recopy-validation.md`。

该窗口首次实现时曾把缩略图改为自适应宽度，导致可缩放图片按原图固有宽度撑出列宽、缩略图互相重叠（弹出面板与历史窗口同时受影响）；已回退为显式固定宽度，并为历史窗口加入列宽不变式测试，防止再次回归。修复后的签名 Release `1.0.1 (2)` 已替换本机安装并启动，替换前备份为 `/Applications/Amano Design/Pic2Link Backups/Pic2Link-1.0.1-2-pre-grid-width-fix-20260912-2326.app`。
